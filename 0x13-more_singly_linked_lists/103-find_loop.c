#include "lists.h"
/**
  * find_listint_loop - find the loop
  * @head: the pointer to frist node 
  * Return: the address of node where loop starts
  */
listint_t *find_listint_loop(listint_t *head)
{
}
