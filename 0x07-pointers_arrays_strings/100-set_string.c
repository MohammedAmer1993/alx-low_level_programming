/**
  * set_string - sets the address of a string to another address
  * @s: the string which will get a new address
  * @to: the new address
  * Return: nothing void func
  */

void set_string(char **s, char *to)
{
	*s = to;
}

